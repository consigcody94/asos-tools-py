name: Deploy to Hugging Face Space

# Mirrors the GitHub main branch into the HF Space git on every push.
# Needed because HF's native GitHub auto-sync silently dropped (the
# Space's `linkedRepo` field went null, and syncs stopped after commit
# 29d8b01).  This workflow removes the dependency on that link by
# force-pushing from GitHub Actions using HF_TOKEN.
#
# Secret required:
#   HF_TOKEN  — write-access HF user access token.  Set via
#               `gh secret set HF_TOKEN -R consigcody94/asos-tools-py`.
#
# Variable used (optional — defaults baked in if unset):
#   HF_SPACE_USER  — HF namespace (default: consgicody)
#   HF_SPACE_NAME  — space slug    (default: asos-tools)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mirror:
    name: Force-push main to HF Space
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout with full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
          persist-credentials: false

      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_USER: ${{ vars.HF_SPACE_USER || 'consgicody' }}
          HF_SPACE_NAME: ${{ vars.HF_SPACE_NAME || 'asos-tools' }}
        run: |
          set -euo pipefail

          if [ -z "${HF_TOKEN:-}" ]; then
            echo "::error::HF_TOKEN secret is not set — cannot mirror."
            echo "Set one with: gh secret set HF_TOKEN -R ${GITHUB_REPOSITORY}"
            exit 1
          fi

          # Configure identity for any re-refs HF creates on its side.
          git config user.email "deploy-bot@asos-tools.ci"
          git config user.name  "ASOS-Tools Deploy Bot"

          # HF Space push — auth via the huggingface_hub CLI, which
          # stores the token in the git credential helper so the
          # subsequent `git push` picks it up.  This is the pattern
          # documented by HF for CI jobs and avoids the "password
          # authentication is no longer supported" error that comes
          # from embedding a user:token pair in the URL.
          python -m pip install --quiet "huggingface_hub>=0.27"
          # The new `hf` CLI supersedes the deprecated `huggingface-cli`.
          hf auth login \
            --token "${HF_TOKEN}" \
            --add-to-git-credential

          HF_URL="https://huggingface.co/spaces/${HF_SPACE_USER}/${HF_SPACE_NAME}"

          # Force-push so GitHub main becomes the canonical HF history.
          # HF's internal commits (if any) are discarded — matches how
          # every deploy worked before the native GitHub link broke.
          git push --force "${HF_URL}" HEAD:main

          echo "::notice::Mirrored ${GITHUB_SHA::7} to ${HF_SPACE_USER}/${HF_SPACE_NAME}"
